-- Aggregate Functions (avg, min, max, count, sum)
select min(age) as avg_age from students;

select max(age) as avg_age from students;

select avg(age) as avg_age from students;

select sum(age) as avg_age from students;

select count(age) as avg_age from students;

select count(*) as avg_age from students;
